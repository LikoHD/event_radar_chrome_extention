# 1.1.4 验证记录

- 首次切入 A/B Test 默认开始监控；手动暂停后切换 tab、清空保留暂停意图。不同浏览器 tab 独立初始化。
- 操作区固定底部，内容独立滚动，AI 按钮仍位于 AI 解读子页。320px 宽度自动化检查底部位置和横向边界。
- 内置 `tea_event_radar/ab-research.json`：15 个网站入口加先前火山控制台，共 16 条。每条含域名、日期、来源、产品背景、历史观察、候选主题、限制与校验步骤。修改此文件后重新构建；它不是自动更新的在线目录。
- AI 准备快照时按域名边界匹配条目，以 researchContext 和 historical-research 证据加入；保留当前参数、时间线、实际上报、疑似线索和脱敏 HTML。历史调研不增加当前实验计数，缺少匹配时只使用当前证据。目录读取失败可降级并说明缺口。
- System Prompt 要求具体参数→可影响功能→候选改动→设计目的→可能分组→主要/护栏指标，并区分历史与当前、事实与假设，说明其他解释。只要求简要、可核查的结论依据，不将推测的另一组当作事实。
- `npm test`：45/45 通过。新增默认监控与暂停保留、研究数据完整性和域名边界、AI 快照历史证据隔离测试。
- 源码与 dist 浏览器冒烟验证：默认采集、单次 AI 发送、扩展 JSON 实际加载、分类跳转、原埋点搜索、设置及窄屏底部布局。
- AI 流程使用模拟接口，没有在本轮向远程模型发送真实网站数据；未重新访问研究中的生产站点，历史观察日期仍为原记录。
